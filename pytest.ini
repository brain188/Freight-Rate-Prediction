[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: needs the assessment CSVs to be present
log_cli = false
filterwarnings =
    ignore::DeprecationWarning